{
  "name": "electron_ipc_renderer",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && rollup -c",
    "pub": "yarn build"
  },
  "dependencies": {
    "is-electron": "^2.2.0",
    "electron": "^10.2.0"
  },
  "jest": {
    "preset": "ts-jest",
    "moduleNameMapper": {
      "electron": "<rootDir>/dist/mock/electron.js"
    }
  },
  "devDependencies": {
    "jest": "26.6.0",
    "ts-jest": "^26.4.4",
    "electron_ipc_main": "*"
  }
}
